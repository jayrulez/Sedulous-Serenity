namespace Sedulous.VG;

/// Line cap style for stroke endpoints
public enum VGLineCap
{
	/// Flat end at the exact endpoint
	Butt,
	/// Rounded end extending by half the stroke width
	Round,
	/// Square end extending by half the stroke width
	Square
}
